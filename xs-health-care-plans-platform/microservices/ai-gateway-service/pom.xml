<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.healthcare.ai</groupId>
    <artifactId>ai-gateway-service</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>AI Gateway Service</name>
    <description>AI Gateway Service - RAG Pipeline for Healthcare Plans</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.10</version>
        <relativePath/>
    </parent>

    <modules>
        <module>ai-gateway-common</module>
        <module>ai-gateway-dao</module>
        <module>ai-gateway-rag</module>
        <module>ai-gateway-service-core</module>
        <module>ai-gateway-api-client</module>
        <module>ai-gateway-api-stub</module>
        <module>ai-gateway-api</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Internal module versions -->
        <ai.gateway.service.version>${project.version}</ai.gateway.service.version>

        <!-- Dependency versions -->
        <springdoc.version>2.8.8</springdoc.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok.version>1.18.36</lombok.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <testcontainers.version>1.20.4</testcontainers.version>
        <aws.sdk.version>2.30.29</aws.sdk.version>
        <opensearch.version>2.19.1</opensearch.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal modules -->
            <dependency>
                <groupId>com.healthcare.ai</groupId>
                <artifactId>ai-gateway-common</artifactId>
                <version>${ai.gateway.service.version}</version>
            </dependency>
            <dependency>
                <groupId>com.healthcare.ai</groupId>
                <artifactId>ai-gateway-dao</artifactId>
                <version>${ai.gateway.service.version}</version>
            </dependency>
            <dependency>
                <groupId>com.healthcare.ai</groupId>
                <artifactId>ai-gateway-rag</artifactId>
                <version>${ai.gateway.service.version}</version>
            </dependency>
            <dependency>
                <groupId>com.healthcare.ai</groupId>
                <artifactId>ai-gateway-service-core</artifactId>
                <version>${ai.gateway.service.version}</version>
            </dependency>
            <dependency>
                <groupId>com.healthcare.ai</groupId>
                <artifactId>ai-gateway-api-client</artifactId>
                <version>${ai.gateway.service.version}</version>
            </dependency>
            <dependency>
                <groupId>com.healthcare.ai</groupId>
                <artifactId>ai-gateway-api-stub</artifactId>
                <version>${ai.gateway.service.version}</version>
            </dependency>

            <!-- Spring Cloud BOM (for Feign) -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- OpenAPI / Swagger -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- AWS SDK -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bedrock</artifactId>
                <version>${aws.sdk.version}</version>
            </dependency>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bedrockruntime</artifactId>
                <version>${aws.sdk.version}</version>
            </dependency>

            <!-- OpenSearch Java Client -->
            <dependency>
                <groupId>org.opensearch.client</groupId>
                <artifactId>opensearch-java</artifactId>
                <version>${opensearch.version}</version>
            </dependency>

            <!-- Testcontainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Lombok for all modules -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
